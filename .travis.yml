language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install request
  - pip install bs4
# command to run tests
script: pytest Threading-time.py